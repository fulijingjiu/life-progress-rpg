# 功能测试清单

更新时间：2026-08-21

本文件与[功能点清单](./功能点.md)使用相同编号。功能“已实现”只表示源码已接入；只有存在可重复执行证据时，测试状态才可写为通过。

## 状态说明

- **通过**：对应自动化检查已执行并通过。
- **部分通过**：部分逻辑有自动化覆盖，完整页面或数据流仍未验证。
- **未验证**：已有实现，但当前没有对应自动化用例或端到端证据。
- **待实现**：功能本身尚未完整实现，暂不具备完整验收条件。

## 自动化基线

执行命令：`npm run test -- --run`、`npm run test:e2e`

当前结果：6 个测试文件、21 项单元测试全部通过；`npm run test:e2e` 的 5 条 Playwright 核心流程全部通过。

| 测试编号 | 对应功能 | 最小测试场景与预期结果 | 当前证据 | 状态 |
| --- | --- | --- | --- | --- |
| T-F001 | 首次引导 | 输入合法信息后保存并进入首页；空昵称允许；非法年份、预期寿命和超长昵称应阻止提交 | [`settings.types.test.ts`](../../src/domain/settings/settings.types.test.ts) + [`core-flows.spec.ts`](../../e2e/core-flows.spec.ts) | 通过 |
| T-F002 | 引导路由守卫 | 未完成引导访问业务页应跳转引导页；已完成引导访问 `/onboarding` 应回首页 | Playwright 从业务页验证引导跳转和完成后业务访问 | 通过 |
| T-F003 | 估算人生进度 | 固定日期计算应得到稳定结果；异常值被限制；关闭开关后首页不显示进度 | [`life-progress.test.ts`](../../src/domain/progress/life-progress.test.ts) + Playwright 设置联动 | 通过 |
| T-F004 | 今日概览 | 无记录显示新增入口；有记录显示心情、活力、回应和更新入口 | Playwright 覆盖无记录与已记录状态 | 通过 |
| T-F005 | 每日记录 | 默认活力与控件一致；合法输入生成指定本地日期载荷；标签去重并限制 6 个；历史记录可回填编辑 | [`record-form.test.ts`](../../src/features/daily-record/model/record-form.test.ts) + Playwright 记录编辑流程 | 通过 |
| T-F006 | 本地持久化与同日更新 | 同一用户同一天连续保存后仅有一条记录；刷新后数据仍存在 | Playwright 验证同日更新、单条约束和刷新回填 | 通过 |
| T-F007 | 本地当日回应 | 有标签时回应引用标签；无标签有备注时引用备注；空内容使用安全默认回应 | [`local-reflection.test.ts`](../../src/domain/records/local-reflection.test.ts) + Playwright 页面断言 | 通过 |
| T-F008 | 可选 AI 整理与失败回退 | 未同意时不得请求 AI；合法响应更新回应；非法响应、网络失败时记录仍保存并标记回退 | 4 项 AI 客户端单测和 Playwright 失败回退通过；真实服务端代理尚未联调 | 部分通过 |
| T-F009 | 回应质量反馈 | 三类反馈均可保存，再次点击当前选项可取消；生成中不可提交反馈 | Playwright 覆盖三类选择与取消 | 通过 |
| T-F010 | 历史记录 | 最近记录按日期倒序且最多 30 条；编辑进入对应日期；确认后删除并刷新；取消确认不删除 | Playwright 覆盖查看、编辑、取消删除和确认删除 | 通过 |
| T-F011 | 设置与隐私偏好 | 合法设置保存并刷新回填；非法值拒绝；关闭 AI 后记录页只能使用本地回应 | 6 项设置单测和 Playwright 保存、刷新、进度开关联动通过 | 通过 |
| T-F012 | JSON 导出 | 导出文件包含格式、schema、导出时间、设置和全部记录，并可被当前导入器重新解析 | Playwright 下载并解析真实导出文件 | 通过 |
| T-F013 | JSON 导入 | 合法备份通过并生成预览；畸形 JSON 和未知格式应拒绝；写入失败不得留下半导入数据 | 3 项解析单测和 Playwright 导出后重新导入通过；事务失败回滚尚未自动化 | 部分通过 |
| T-F014 | 清空本地数据 | 任一次确认取消都不清空；两次确认后设置和记录删除 | Playwright 覆盖取消和双重确认清空 | 通过 |
| T-F015 | 主导航与未知路由回退 | 四个导航入口可达；当前页有活动状态；未知地址回到首页 | Playwright 覆盖主导航、未知地址和四种视口无横向溢出 | 通过 |
| T-F016 | 离线使用 | 页面已加载后断网仍可保存；恢复联网并刷新后记录仍存在 | Playwright 使用浏览器离线模式验证保存与恢复 | 通过 |
| T-F017 | 主题切换 | 选择每个主题后界面立即变化，刷新后保持；非法主题回退默认值 | Playwright 覆盖 5 个主题切换及持久化，设置模型负责非法值回退 | 通过 |
| T-F018 | 连续记录统计 | 跨本地日期连续记录正确计数；缺失日期后重新计数；不产生断签惩罚文案 | 功能未实现 | 待实现 |
| T-F019 | 进度动画与 Reduce Motion | 默认模式动画不阻塞交互；启用 `prefers-reduced-motion` 后取消非必要动画 | 功能未实现 | 待实现 |

## 通用回归命令

```bash
npm run lint
npm run test -- --run
npm run test:e2e
npm run build
python .codex/skills/implement-life-progress-rpg/scripts/validate_project.py
git diff --check
```

浏览器、可访问性和性能证据统一记录在[验收证据](../验收证据/README.md)，不在本文件复制截图清单。

[返回文档中心](../README.md)
